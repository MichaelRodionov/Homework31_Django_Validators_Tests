# Homework31_Django_Validators_Tests

**To start project open and run utils.py. This script will start postgres container, convert csv to json, make migrations, load data to database and start server**
